<%= form_for([@attribute_category, @character_attribute]) do |f| %>
  <%= display_errors @character_attribute %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
