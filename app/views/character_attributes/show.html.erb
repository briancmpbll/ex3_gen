<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @character_attribute.name %>
</p>

<%= render "abilities/list" %>

<%= link_to 'Edit', edit_attribute_category_character_attribute_path(@attribute_category, character_attribute) %> |
<%= link_to 'Back', attribute_category_path(@attribute_category) %>
