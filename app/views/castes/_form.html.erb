<%= form_for(@caste) do |f| %>
  <% if @caste.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caste.errors.count, "error") %> prohibited this caste from being saved:</h2>

      <ul>
      <% @caste.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.label :anima_effect %><br>
    <%= f.text_area :anima_effect %><br>
    <%= f.label :character_type %>
    <%= f.collection_select :character_type, CharacterType.order(:name), :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
